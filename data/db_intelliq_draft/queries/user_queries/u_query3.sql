/*          USER QUERY 2 --> c. {baseURL}/doanswer/:questionnaireID/:questionID/:session/:optionID     */

INSERT INTO Answer (session, optID) VALUES ('{session}', '{optID}');

/* Looks good to me assuming that the session exists.*/
